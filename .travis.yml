language: node_js
sudo: true
before_install:
  - echo -e "Host *\n\tStrictHostKeyChecking no\n" >> ~/.ssh/config
install:
  - npm install 
  - npm install -g gulp
before_script:
- gulp build
script:
- gulp test
- ./.travis/deploy.sh
