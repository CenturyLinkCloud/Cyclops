describe 'Widgets: actionToolbarConfirm', ->
  beforeEach jasmine.prepareTestNode
  it 'hangs off the global jquery fn object', ->
    expect(jQuery.fn.actionToolbarConfirm).toBeDefined()
    return
  return
